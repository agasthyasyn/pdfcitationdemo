[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  # Firebase client-side config values are intentionally public
  # (security enforced via Firebase Security Rules, not key secrecy).
  # Disable smart detection to prevent false positives on these values.
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
